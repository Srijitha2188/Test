import random                           //Generating random no's

print(random.randint(0,9))

num = int(input("Enter a number: "))   //Checking the no is even or odd
if (num % 2) == 0:
   print("{0} is Even".format(num))
else:
   print("{0} is Odd".format(num))
